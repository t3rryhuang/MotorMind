from django.apps import AppConfig


class StudyContentConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "study_content"
    verbose_name = "Course reading & study context"
